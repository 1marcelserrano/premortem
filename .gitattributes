# Auto detect text files and perform LF normalization
* text=auto

# Esconde tooling auxiliar da detecção de linguagem do GitHub —
# o repo se apresenta pelo conteúdo, não pelos assets de preview.
assets/*.html linguist-vendored
page/*.html linguist-vendored
